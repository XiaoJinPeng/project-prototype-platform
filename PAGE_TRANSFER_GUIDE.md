# 页面导入导出说明

## 定位

页面导入导出是本地工程工具，用来衔接需求阶段的 HTML 原型与工程阶段的 Vue 页面。

- 导入：接收由 `templates/html-prototype-page.html` 创建的规范 HTML，或本平台导出的可回导 HTML，并生成工程内的 Vue 页面、路由和菜单登记。
- 导出：从当前页面注册表选择一个或多个页面；如果页面配置了原始 HTML 来源，则直接复用该 HTML 生成轻量导出文件，同时保留可回导的 Vue 页面源文件区。
- 轻量导出保留原 HTML 中的 Vue、Element Plus、弹窗、表单和页面脚本交互，不再把工程 Vue 运行包重新编译并嵌入 HTML。
- HTML 直读是独立的第二种页面来源，不改变导入导出流程；直读页面只进入原型路由，不参与 Vue 页面导出。
- 平台导出 HTML 直读时会优先使用回导 Manifest 中的客户端、路由、菜单分组、图标和菜单可见性；如果目标项目已有同路径页面，平台会为直读页面生成不冲突的路径。
- HTML 直读目录在项目配置中按客户端设置；每个客户端可以使用不同的外部 HTML 文件夹。
- 历史 HTML 页面不纳入自动清理、自动迁移或批量重构范围。

## 使用入口

启动工程开发服务后，打开首页，在底部“工程工具”中进入“页面导入导出”，或直接访问：

```text
/tools/page-transfer
```

```powershell
npm run dev
```

导入和导出都使用当前 Vite 开发服务提供的本地 Node 能力，不需要额外启动第二个服务。Node 只在本机工程侧使用，导出的轻量 HTML 不要求接收方安装 Node，但打开页面交互时需要能够访问 HTML 中声明的 CDN。

## 导入流程

1. 在 HTML 原型阶段复制 `templates/html-prototype-page.html`，只编辑模板规定的 `[AI-EDIT]` 区域。
2. 页面定案后，在页面导入导出工具中上传 HTML。
3. 工具检查 Manifest、页面内容区、业务内容区、覆盖层、页面逻辑和页面专属样式。
4. 选择目标项目，并选择“新增页面”或“替换页面”。
5. 新增时填写客户端、路由片段、菜单分组、菜单名称和图标；替换时先选择原页面，系统会自动带出这些信息，仍允许修改。
6. 点击导入。新增会生成新 Vue 页面；替换会先把原 Vue 文件和原页面定义保存到项目包的 `.backups/pages`，再写入新页面。
7. 重新载入开发服务后，页面会出现在对应路由和菜单中。被替换页面的备份可在“路由菜单管理”中还原。

导入时页面内容、弹窗、抽屉、页面逻辑、模拟数据和专属样式作为一个整体处理，不允许只选择部分业务区块。原因是区块之间可能共享状态、计算属性和事件方法，任意拆选容易造成页面可编译但交互失效。

## 导入校验边界

必须满足：

- `templateVersion` 为 `1`。
- `pageKey` 使用小写 kebab-case。
- `client` 使用小写 kebab-case，并且必须存在于所选项目的 `project.json.clients`。
- `routePath` 使用 `/{client}/{path}`。
- 页面只能有一个实际的 `data-page-content` 节点。
- 页面包含内容区、覆盖层和页面逻辑标记。
- 不使用 `onclick` 等内联事件。
- 菜单分组和图标必须属于当前工程已有配置。

外部图片、外部脚本和不建议使用的浏览器跳转逻辑会产生警告。模板要求的页面状态、方法和模拟数据仍然保留在生成页面内部；不会自动抽取到公共 Mock 文件。

## 导出页面

1. 选择要导出的项目，再按客户端筛选页面。
2. 勾选一个或多个页面。
3. 填写导出名称。
4. 只选一页时点击“导出单页 HTML”；选择多页时点击“生成多页 ZIP”。

有原始 HTML 来源的页面不会再走 Vite 独立应用编译；导出会复用原 HTML 的结构、样式、脚本和交互，只注入页面 Manifest、可回导源码区和多页导出配置。不会把整个工程、PRD 文档或未选页面打入导出文件。多页导出会根据选择结果改写页面间相对链接。

同一份平台导出 HTML 再次参与导出时，生成器会先移除旧的 Manifest、导出配置、可编辑源码区和直读裁剪样式，再写入一套最新区块，保证标准 HTML `id` 唯一。多页链接优先按源文件的真实相对路径匹配；只有路径无法解析且文件名在所选页面中唯一时，才使用文件名兜底，避免不同目录的同名页面互相串页。

只选择一个页面时，直接输出一个独立 HTML，不生成 ZIP：

```text
页面名称.html
```

选择多个页面时，每个页面生成一个可独立打开的 HTML，并统一压缩为 ZIP：

```text
页面演示包.zip
├─ operation-page-a.html
├─ operation-page-b.html
└─ enterprise-page-c.html
```

多页文件中的侧栏菜单和原页面 HTML 链接使用相对文件名互相跳转。页面专属 CSS、JavaScript 和工程图片会随 HTML 输出，不生成 `assets` 文件夹；公共依赖通过 CDN 加载，因此接收方不需要安装 Node，但需要网络连接。页面运行文件不会连接真实接口，也不会包含真实登录、权限或数据持久化。

导出的每个 HTML 会包含标准 HTML 文档结构、页面元数据和可编辑源文件区。`prototype-page-manifest` 保存页面元数据；`prototype-editable-template`、`prototype-editable-script`、`prototype-editable-style` 分别保存格式化的 Vue 模板、逻辑和样式，并使用 `[AI-EDIT]` 标记说明编辑边界。重新上传时，导入器只读取这三个可编辑源文件区；页面可见运行区仍是原始 HTML，不是重新编译的工程运行快照。由于源文件可能引用当前项目的页面资源和模拟数据，回导优先用于同一项目内的页面替换或同路径恢复。

导出完成后，服务端会先将生成文件写入 `exports/{project-id}`，同时返回预览地址和下载地址。点击下载按钮时由浏览器通过下载接口接收文件；`exports` 只是本地预览和下载所需的服务端输出目录，可定期清理旧文件。

## 路由菜单管理

首页“工程工具”中的“路由菜单管理”用于逐个项目管理客户端页面登记：

- 新增路由时填写客户端、路由片段、页面名称、菜单分组和图标，系统会同时生成一个可运行的占位 Vue 页面。
- 编辑路由时可以修改页面名称、路由片段、菜单分组和图标；如果修改路由片段，系统会同步调整页面文件路径，但不会改动页面内容。
- 编辑菜单分组时可以新增分组、重命名已有分组或删除分组；已有分组 ID 和页面归属关系保持不变，仍被页面使用的分组不能删除。分组修改会单独生成备份。
- 删除路由时会从页面定义和菜单中移除，并将原 Vue 文件及页面定义保存到 `projects/{project-id}/.backups/pages/{backup-id}`。
- 编辑和替换导入产生的原页面备份也会出现在这里，可点击“还原”恢复原页面和原路由登记。
- 备份还原后仍保留记录并标记为“已还原”，不会自动清理历史备份。
- 删除客户端默认页面会被阻止，需要先在项目配置中更换默认页面，避免项目包进入无效状态。

## 版本和来源关系

HTML 原型是需求定案前的页面来源；导入完成后，工程中的 Vue 页面成为工程维护来源。轻量导出会同时携带原始 HTML 展示文件和 Vue 回导源码区，适合交给 AI 修改后再次导入，但仍建议最终只维护一份正式来源。

如果需求仍在变化，应优先修改原 HTML 原型或 Vue 页面正式来源，再重新导出。导出 HTML 中只修改 `prototype-editable-*` 区块，避免直接修改运行脚本和元数据结构。不要同时把导出 HTML 和 Vue 页面当成两套长期维护源。

## 当前限制

- 规范 HTML 可以导入任意项目包中已登记的后台客户端，不支持移动端多画面 HTML 自动拆分。
- 导入暂不自动复制 HTML 文件旁边的任意资源目录；模板页面应优先使用工程已有资源或内联资源。
- 有原始 HTML 来源的轻量导出只包含原 HTML、页面源码区和少量导出元数据，文件体积接近原始 HTML，明显小于完整工程包。
- 如果客户现场无法访问 CDN，页面无法加载 Vue、Element Plus 或字体；此时应使用开发服务或另行制作离线资源包，不要直接把 CDN 版 HTML 当作离线包。
- 页面注册表写入后需要重新载入开发服务，才会重新生成路由和菜单模块。

## 版本记录

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| v1.8 | 2026-07-24 | 收敛重复导出区块、真实路径链接改写和原型目录单次索引；修复 script setup、scoped、lang 和 CSS Module 回导属性。 |
| v1.7 | 2026-07-22 | 优先复用原始 HTML 生成轻量导出，保留页面交互并改写多页相对链接，避免重复编译 Vue 运行包。 |
| v1.6 | 2026-07-21 | 增加格式化的可编辑 Vue 模板、逻辑和样式区块，导出 HTML 可交给其他 AI 修改后继续回导。 |
| v1.5 | 2026-07-21 | 统一导出标记解析，直读复用导出路由与菜单元数据，补齐导出-直读-回导闭环。 |
| v1.4 | 2026-07-21 | 导出 HTML 保留可回导 Vue 源文件区，导入器支持识别并恢复导出页面。 |
| v1.3 | 2026-07-16 | 扩展菜单分组管理，支持新增、重命名、删除及分组备份还原。 |
| v1.2 | 2026-07-16 | 增加菜单分组重命名、分组备份与还原说明。 |
| v1.1 | 2026-07-16 | 增加新增/替换导入、原页面备份和路由菜单管理说明。 |
| v1.0 | 2026-07-16 | 建立 HTML 原型导入和独立 HTML/ZIP 导出规范。 |
